using UnityEngine;
using UnityEditor;
using System.Text;
using System.IO;
using UnityObject = UnityEngine.Object;

namespace My3DGame
{
    /// <summary>
    /// 데이터 툴과 관련된 공통기능 구현
    /// </summary>
    public class EditorHelper
    {
        //등록한 데이터(UnityObject)의 Path(경로) 가져오기
        public static string GetPath(UnityObject p_Clip)
        {
            return null;
        }

        //툴에 있는 데이터의 이름 리스트로 enum 만들기
        public static void CreateEnumStructure(string enumName, StringBuilder data)
        {

        }

        //데이터 툴의 상단 레이어(Add, Copy, Remove 버튼) 만들기

        //데이터 이름 목록 리스트 레이어 만들기
    }
}